/** What a hero skill does when active. Stored in `HeroSkill.effect_type`. */
export enum SkillEffect {
  /** A wrong answer (or timeout) on the current question costs no lives. */
  BlockWrongAnswerDamage = 1,
}
